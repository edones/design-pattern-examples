package behavioral.visitor;

public class VisitorDemo {
    public static void main(String[] args) {
        //Needs: Visitor, Element, ConcreteVisitor, ConcreteElement
        //1. create element interface & concrete element
        //2. add the 'accept(Visitor visitor)' method in element interface
        //     with hook in body:   visitor.visit(this);
        //3. create visitor interface
        //4. add concrete visitor
    }
}
